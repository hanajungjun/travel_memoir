name: daily_word_admin
description: "Daily word admin web panel"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  # Supabase (DB + Storage)
  supabase_flutter: ^2.4.0

  # 웹에서도 동작하는 파일 선택
  file_picker: ^8.0.0

  flutter_html: ^3.0.0

  # 로컬 알림 사용 X (admin web은 필요 없음)
  # → 필요하면 다시 추가 가능

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
